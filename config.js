const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "",
        database: "ml-api-test",
        coonectTimeout: 60000
    },

    listPerPage: 10,
};

module.exports = config;