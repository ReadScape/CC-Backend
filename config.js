const config = {
    db: {
        host: "readscape.live",
        user: "readscape",
        password: "gustira1708",
        database: "readscape",
        connectTimeout: 60000
    },

    listPerPage: 10,
};

module.exports = config;